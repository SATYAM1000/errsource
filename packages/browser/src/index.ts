// @satyamx55/errsource-browser — coming next:
// window.onerror + unhandledrejection listeners that report
// { message, stack, release: window.__ERRSOURCE_RELEASE__, url, userAgent }
// to the errsource server's /api/errors endpoint.
export {};
