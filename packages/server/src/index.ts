// @satyamx/errsource-server — coming next:
// POST /api/sourcemaps  — authenticated upload endpoint used by the Vite plugin
// POST /api/errors      — public ingest endpoint used by the browser SDK
// symbolication: stack trace parser + source-map lookup + Slack alerts
export {};
